apiVersion: snowstrike/v1
kind: Tool
metadata:
  name: ghidra_get_metadata
  category: session_mgmt
  requires_root: false
  passive: false
  transport: http
  endpoint: /get_metadata
  http_method: GET
spec:
  description: 'Get metadata about the loaded program: architecture, format, compiler,
    entry point, memory layout.'
  input_schema:
    type: object
    properties: {}
    required: []
  compatible_agents:
  - ghidra
