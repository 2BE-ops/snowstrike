apiVersion: snowstrike/v1
kind: Tool
metadata:
  name: ghidra_list_imports
  category: symbols
  requires_root: false
  passive: false
  transport: http
  endpoint: /list_imports
  http_method: GET
spec:
  description: List all imported symbols and their source libraries (e.g., libc functions,
    Windows API calls).
  input_schema:
    type: object
    properties: {}
    required: []
  compatible_agents:
  - ghidra
