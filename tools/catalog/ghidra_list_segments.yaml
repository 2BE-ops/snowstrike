apiVersion: snowstrike/v1
kind: Tool
metadata:
  name: ghidra_list_segments
  category: data_analysis
  requires_root: false
  passive: false
  transport: http
  endpoint: /list_segments
  http_method: GET
spec:
  description: List all memory segments (sections) in the program with addresses,
    sizes, and permissions.
  input_schema:
    type: object
    properties: {}
    required: []
  compatible_agents:
  - ghidra
