apiVersion: snowstrike/v1
kind: Tool
metadata:
  name: ghidra_save_program
  category: session_mgmt
  requires_root: false
  passive: false
  transport: http
  endpoint: /save_program
  http_method: POST
spec:
  description: Save the current program state (renames, comments, type changes) to
    disk.
  input_schema:
    type: object
    properties: {}
    required: []
  compatible_agents:
  - ghidra
